# requirements.txt

# Core
python-dotenv==1.2.2

# Data handling
pandas==3.0.3
numpy==2.5.1

# Modeling
scikit-learn==1.9.0
scipy==1.18.0

# Persistence
SQLAlchemy==2.0.51

# Config validation
pydantic==2.13.4
PyYAML==6.0.3

# App / viz (needed from Phase 9, pinned now for reproducibility)
streamlit==1.59.2
plotly==6.9.0

# API layer (optional, Phase 8)
fastapi==0.139.0
uvicorn==0.51.0

# Testing
pytest==9.1.1

# NOTE: causalml is intentionally NOT pinned here yet.
# Required starting Phase 5 (evaluation cross-check). Confirmed compatible:
# causalml 0.17.0 ships a genuine cp312 wheel (verified against actual
# release filenames, not just declared metadata) and requires numpy>=1.25.2,
# scipy>=1.16.0, scikit-learn>=1.6.0 -- all satisfied by the pins above.
# It DOES pull a heavy dependency tree (xgboost, lightgbm, shap, statsmodels,
# matplotlib, seaborn, forestci, pathos, graphviz) -- fine on 3.12, revisit
# the lightweight-alternative (scikit-uplift) decision in Phase 5 only if
# the install proves too heavy in practice, not due to version conflicts.